$(function(){

    'use-strict';

    var showReel = $('#showreel-container');

    /*var controller = new ScrollMagic.Controller();

    new ScrollMagic.Scene({
            triggerElement: showReel.get(0),
            triggerHook: 'onCenter',
            duration: 300
        })
        .setTween('img', {opacity: 0, y: '-200' })
        .addTo(controller);*/

});